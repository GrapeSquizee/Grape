# 전체 의존성 — 폐쇄망에서는 내부 Nexus(PyPI 미러)로 설치.
# 패키지 외 추가 반입: PaddleOCR 모델(~/.paddlex/official_models), 한글 폰트.
# 대체 OCR 엔진 사용 시 OS 패키지: tesseract-ocr, tesseract-ocr-kor

# 코어 (탐지·합성) — 순수 파이썬, faker 없이도 내장 목록으로 동작
faker

# 이미지 처리 (전처리·렌더링)
pillow
opencv-python-headless
numpy

# OCR (주력: PaddleOCR / 대체: pytesseract + OS tesseract)
paddleocr
paddlepaddle
pytesseract

# 개발·테스트
pytest

# QA 검수 (별도 서버에 설치 권장)
# label-studio
